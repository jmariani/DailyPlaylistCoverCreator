#!/usr/bin/env ruby

require "pathname"

APP_ROOT = Pathname.new(__dir__).join("..").realpath

Dir.chdir(APP_ROOT)

require "bundler/setup"
require APP_ROOT.join("lib/daily_playlist_cover_creator/cli").to_s

exit DailyPlaylistCoverCreator::CLI.run(ARGV)
